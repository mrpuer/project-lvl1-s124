import makeGame from '../';

const brainGames = (i) => {
  const rules = '';
  const makeQuestion = () => {};
  const makeAnswer = () => {};
  makeGame(rules, makeQuestion, makeAnswer, i);
};
export default brainGames;
