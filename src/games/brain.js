import makeGame from '../';

const brainGames = (i) => {
  const rules = '';
  const makeData = () => {};
  const makeQuestion = () => {};
  const makeAnswer = () => {};
  makeGame(rules, makeData, makeQuestion, makeAnswer, i);
};
export default brainGames;
